apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: workload
  namespace: bench
  labels:
    release: prom
spec:
  selector:
    matchLabels:
      app: workload
  endpoints:
  - port: http
    path: /metrics
    interval: 15s
