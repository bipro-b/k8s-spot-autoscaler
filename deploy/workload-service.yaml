apiVersion: v1
kind: Service
metadata:
  name: workload
  namespace: bench
  labels:
    app: workload
spec:
  type: NodePort
  selector: { app: workload }
  ports: [{ name: http, port: 80, targetPort: 8080, nodePort: 30080 }]
